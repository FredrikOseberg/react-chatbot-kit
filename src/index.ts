import Chatbot from "./components/Chatbot/Chatbot";
import {
  createChatBotMessage,
  createClientMessage,
} from "./components/Chat/chatUtils";

export { Chatbot, createChatBotMessage, createClientMessage };

export default Chatbot;
